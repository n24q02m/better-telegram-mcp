# Code Review Request: [TEST] Missing test for auth_client _handle_command HTTPError

## Changes
- Created `tests/test_auth_client.py` with 100% coverage for `src/better_telegram_mcp/auth_client.py`.
- Tested success and error paths for:
  - `create_session`
  - `poll_and_execute` (including `httpx.HTTPError` and general `Exception`)
  - `_handle_command` (send_code, verify)
  - `_push_result`
  - `close`
- Removed `*/auth_client.py` from `omit` in `pyproject.toml` to track coverage.

## Verification
- `uv run pytest tests/test_auth_client.py` passes.
- `uv run pytest --cov=better_telegram_mcp` passes with 98.13% total coverage.
